var chai = require("chai");
var assert = chai.assert;
var dupeFind = require('./app.js');

describe("A function to make an array that contains every word repeated more than once", () => {
  it('Should return true if a word is repeated', () => {
    assert.equal(dupeFind('This should be the '));
  })
  it('')
})
